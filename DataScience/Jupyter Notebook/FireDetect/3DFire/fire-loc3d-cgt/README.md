# FIRE-LOC3D (Phase 1)

Dual-camera 3D localization of **fire base** / **smoke origin** from your YOLOv10 detector.

## Install

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```
